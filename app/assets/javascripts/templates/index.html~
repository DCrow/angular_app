<div class="container-fluid" ng-app="angular_app">
  <div class="panel panel-success">
    <div class="panel-heading">
			<li ng-repeat="user in users">
      <h1 ng-if="name">Hello, {{User.name}}</h1>
			</li>
    </div>
  </div>
</div>

